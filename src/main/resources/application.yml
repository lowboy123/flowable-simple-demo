server:
  port: 8081

spring:
  datasource:
    url: jdbc:mysql://127.0.0.1:3306/flowtest?useUnicode=true&characterEncoding=utf-8&zeroDateTimeBehavior=convertToNull&useSSL=false
    username: root
    password: root
    driver-class-name: com.mysql.jdbc.Driver

#暴露端口
management:
  endpoints:
    web:
      exposure:
        include: env,refresh

testData: 123!!!